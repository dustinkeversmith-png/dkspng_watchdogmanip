
# Database Viewer Application

1. What type of database do I have, where can I find it
.database.parsed_commands.sqlite


2. Develop the details/api usage of the database
3. Develop it, test it
4. One shot finally the viewer system/api

How to build my application here, make it callable via capabilities, possibly switching it on cli demand base or daemon based, so it can be spawned up, design an adapter around it or a generic cli adapter for controlling and application that has cli controls

6. One shot finally the capability pipeline train system

# Json Query Viewer Application

Extension to the previous application

# Environment Pipeline Edition


Pipeline Format

"parse" -> json-viewer
	"command" -> command_ref -> json-viewer
		"location" -> file-viewer
			"db_refs" -> db-viewer

# Required Tests


1. Application registry-id system for handling, with potential query formats
2. Setup layout with applications
3. Iterate through and execute the nested hierarchy
4. With a pipeline configuration such as the one above



# Integration Test

Compile Documentation Describing Software Particularly API to the viewer
Create a capability for the viewer including networking module for the automation center
Create the pipeline config file and then run the config timeline template


# Test the reader, understand how to communicate with it