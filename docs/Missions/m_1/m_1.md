
# Parser Development

- Set up data base for parser elements

# Preface

Multiproject file hierarchy searching and parsing, need a tree adequate tree walker with settings for inclusions etc, and then need to figure out what a good database design would be useful for, need a editor, organizer and extractor for testing (done)

# Create a scriptable runnable executable test environment

cargo test --test parse -- --nocapture
Inside of parse there should be some interesting information
# tests/parse/test_walk_tree and execution component

src/walk/tree_walker.rs

# Feature Sets

1. Need inclusion and exclusion patterns for subdirectoy types like .gitignore, node modules etc


# Ensure the database is usable, ensure the tree walker component works

Database Location

src/database/parsed_commands.sqlite

let db = CommandSqliteDatabase::open_existing("src/database/parsed_commands.sqlite")?;

cargo test --test database -- --nocapture


# Fully Deck out the Parse Text

1. Log the full walk out and explore its walk efficacy
	1. Using the underlying navigation engine to use the underlying capability engine ensuring that explorer navigation process management is available
2. Log the full parsing, add the file reference path and location
	1. Using the underlying navigation engine and underlying capability engine ensuring that the code editor and navigations are available, along side, viewing the parsed data in one window and the cooresponding database entry data in another window
3. Print out the database tables for future usage building a database test search or vectorization improvement test
4. Evaluate with automatic navigation testing the navigation unit


# Steps
1. Design a deeply nested multiproject file hierarchy filled with commands and parsing information, similar or identical to what is currently in your projects folder
	1. The files should have route directories in different places for example, I will have seeds on my desktop and spread in locations similar how my thing currently works
2. Add or connect a parsing, and data collection protocol to this search and parse recorvery, figure out a good data base for this
3. Develop a database search/handler schema for usage
4. Develop a editor/file editor for possibly removing or expanding these possibly out of context commands etc

# Navigation Testing

- Use these for navigation and aliasing testing
- Use these for organization and routine testing
