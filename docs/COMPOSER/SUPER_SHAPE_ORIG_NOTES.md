I wonder if it would be advantagous to collect more information about the boundary types, possibly using the boundary types for help full estimation of things, boundary meta information, might be able to help tell how or where to extract bodies for certain tags or commands

commands
body and titles, how to infer best like the shape of these parameters or like expected shape in a weird way, so I need some kind of like general shape identifier, as well as possible titles in the commands

then can add tests for these in the block parsing

everything should always be loose get rid of allow lose body, just add parameters then add optional tag to members or parameters

# Parser seed command detection
1. Need to include the concept of like a classifier keyword, optionally detecting chained commands without explicit @symbols using keyboard searches, having a status or other identifier glossary identifier, such as general parameter keywords or identifiying if the chained word is a command type if so what kind of command type info is it


# Hierarchy detector needs attachable hierarchy detectors, for example numbered lists that start over, indent levels, bullet points or other kinds of detect, the hierachy detector should then be able to like assign parent ids or some kind of topology on it

# I also need a test for testing body parsing to see how that works

# also each command or parse should also house with file location if possible ontop of any other location information so that can be tracked

